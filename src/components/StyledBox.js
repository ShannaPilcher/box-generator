import React from 'react';
import styled from 'styled-components';
 
const StyledBox = (props) => {
    return (
        <div style= {props.colorObj} >

        </div>
    )
}
 
export default StyledBox;
